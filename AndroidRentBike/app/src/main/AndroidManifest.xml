<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ro.ubbcluj.scs.bnie1869.rentbike">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin.ContactActivity" />
        <activity android:name=".admin.ViewListActivity2" />
        <activity android:name=".admin.DetailsActivity" />
        <activity android:name=".admin.CreateActivity" />
        <activity android:name=".user.UserViewListActivity" />
        <activity android:name=".user.UserDetailsActivity" />
        <activity android:name=".common.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.UserContactActivity"></activity>
    </application>

</manifest>